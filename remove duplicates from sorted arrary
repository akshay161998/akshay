class Solution:
    def removeDuplicates(self, nums):
        if not nums:
            return 0

        insert_nos=1
        for i in range(1, len(nums)):
            if nums[i]!=nums[i-1]:
                nums[inser_nos]+=nums[i]
                insert_nos+=1
                return insert_nos 
