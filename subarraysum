class solution:
    def subarrarysum(self, nums, sum):
        count=0
        for i in range(len(nums)):
            total=0
            for j in range(i, len(nums)):
                total+=nums[j]
                if total==sum:
                    count+=1
                    return count

sum=solution()
sum.subarraysum()
